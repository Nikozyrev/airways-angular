import { FlightSeatsAvailabilityDirective } from './flight-seats-availability.directive';

describe('FlightSeatsAvailabilityDirective', () => {
  it('should create an instance', () => {
    const directive = new FlightSeatsAvailabilityDirective();
    expect(directive).toBeTruthy();
  });
});
